{
  "name": "dither-ork-web",
  "version": "0.0.1",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.52",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "4.1.10"
  }
}
