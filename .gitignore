# Rust
core/target/
**/*.rs.bk

# Generated WASM package — built by `docker compose up wasm`
web/src/wasm/pkg/

# Node
web/node_modules/
web/dist/
npm-debug.log*

# The manual browser probe, staged out of web/test/probe/ by `stage.cjs`.
# Everything in public/ is copied into dist/, and the fixtures are megabytes of
# test images that have no business in a production build.
web/public/probe/

# Editors / OS
.DS_Store
.idea/
.vscode/

# Wrangler writes cache and account metadata here.
.wrangler/
